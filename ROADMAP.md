# 株レポート 機能追加ロードマップ

## 制約前提

| 項目 | 内容 |
|------|------|
| Render 無料枠 | RAM 512MB / 15分無操作でスリープ / 永続ストレージなし |
| かぶたん | 1銘柄あたり13並列リクエストが現状の上限ライン |
| Yahoo Finance | crumb方式で不安定。メインデータはかぶたんに移行済み |
| キャッシュ | サーバー側 InMemoryCache（スリープで消える） + localStorage（1時間） |

---

## 対応済みの修正・改善（2025-04）

### 矛盾点
- [x] ファンダメンタルスコアの正規化式バグ修正 `(points+6)/17 → (points+5)/15`
- [x] ATR高ボラを上昇トレンド中のみ買い根拠として表示
- [x] 総合シグナルを3段階→5段階化（strong_buy / strong_sell 追加）
- [x] リスクリワード評価テキストに isBuy 分岐を追加
- [x] 報酬目標を ATR ベース前向き計算に変更
- [x] 信用倍率「低」をスコアに反映・売り警告に追加

### 改善点
- [x] かぶたんフェッチを逐次→並列（Promise.all）
- [x] BB/RSI/MACD をサーバーで履歴計算・クライアント二重計算を排除
- [x] 価格帯に応じた動的刻み幅（固定100円→10/50/100/500円）
- [x] 注目ポイントに一目雲下限・52週高値安値を追加
- [x] importance=5 を導入（SMA50サポート・52週高値近接）
- [x] app.js のデッドコード削除（computeBBHistory 等）

---

## 追加候補機能

### フロントエンドのみ（APIリクエスト増なし）

#### ウォッチリスト
- localStorage に銘柄コードを最大20件保存
- サイドバーまたはドロップダウンで一覧表示
- ワンクリックでレポート取得
- **優先度: ★★★★★**

#### 銘柄コード履歴
- 直近10銘柄をドロップダウン表示
- ウォッチリストと組み合わせ実装が自然
- **優先度: ★★★★★**

#### チャート期間切替
- 取得済みデータの表示範囲を切り替え（1M / 3M / 6M / 1Y）
- DataZoom の start/end を変えるだけで実装可能
- **優先度: ★★★★☆**

#### 前回スコア比較
- localStorage に前回スコア・シグナルを保存
- 今回との差分（例：`BUY ↑ +8pt`）をヘッダーに表示
- **優先度: ★★★☆☆**

#### 印刷 / PDF 出力
- `window.print()` + 印刷用 CSS でレポートを出力
- **優先度: ★★☆☆☆**

---

### サーバー側計算追加（追加スクレイピングなし）

#### チャートパターン検出
- 既存価格データから以下を検出してレポートに追加
  - ゴールデンクロス / デッドクロス（SMA20 × SMA50）
  - 直近N日の高値・安値の切り上げ / 切り下げ
- **優先度: ★★★★☆**

#### 出来高異常検知
- 直近出来高が20日平均の2倍超を検知
- レポートに「出来高急増」シグナルとして表示
- **優先度: ★★★★☆**

#### 季節性分析
- 既存252日データから月別平均リターンを計算
- 例：「過去1年で3月の平均リターンは +2.1%」
- **優先度: ★★★☆☆**

#### PEG レシオ
- `PER / EPS成長率` を既存データから計算
- 1.0以下で割安成長株の目安として表示
- **優先度: ★★★☆☆**

#### 価格ボラティリティ分類
- 過去30日の標準偏差で「低 / 中 / 高ボラ株」を分類表示
- **優先度: ★★☆☆☆**

---

### かぶたんから1〜2リクエスト追加（ブロックリスク低）

#### 決算スケジュール
- 次回決算予定日を取得してレポートに表示
- 決算2週間以内は「決算前注意」バナーを表示
- **優先度: ★★★★☆**

#### 株主優待情報
- 優待の有無・内容・権利確定月をかぶたんから取得
- **優先度: ★★★☆☆**

#### 空売り比率
- かぶたんの需給情報ページから追加取得
- 信用倍率と組み合わせて需給分析を強化
- **優先度: ★★★☆☆**

---

## 実装しない機能（理由あり）

| 機能 | 見送り理由 |
|------|-----------|
| 複数銘柄一括取得 | かぶたんへのリクエスト数が銘柄数 × 13 に膨れる |
| 業種平均比較 | 業種内全銘柄取得が必要で過負荷 |
| リアルタイム株価 | スクレイピングでは遅延あり・WebSocketは過負荷 |
| キャッシュ永続化（DB） | Render 無料枠に永続ストレージなし |

---

## おすすめ実装順

1. **ウォッチリスト + 銘柄コード履歴** — 使い勝手が最も変わる・実装コスト最小
2. **チャートパターン検出（GC/DC）** — レポートの付加価値向上
3. **決算スケジュール** — 決算前後のボラ急上昇を事前把握
4. **出来高異常検知** — 既存データのみで実装完結
5. **チャート期間切替** — UI改善として自然な追加
